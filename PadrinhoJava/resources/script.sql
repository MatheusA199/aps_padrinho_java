create database pessoa_fisica;

create table pessoa_fisica(

id int not null auto_increment primary key,
nome varchar(255),
email varchar(255),
senha varchar(25),
dateCadastro date)

create database transacao;

create table pessoa_fisica(

id int not null auto_increment primary key,
nome varchar(255),
email varchar(255),
senha varchar(25),
dateCadastro date)